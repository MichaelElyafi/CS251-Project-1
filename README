

header.c : 

    included by actual test programs tX.c

    contains macros for running tests and tabulating scores.

    also contains several utility functions for building test cases
    without relying on student code.

    also contains "calibration" code for calculating reasonable 
    elapsed time limits for certain operations.

test programs:

    t1.c ... t9.c

    each tests 1-3 of the assigned functions for the assignment.


INSTRUCTIONS:
    1. copy your files into this folder
    2. make sure you have llist.c and list.h files included
    3. run the following command:
        make -f makefile_tests clean
        make -f makefile_tests tests
    4. test each individual files, for example:
        ./t1
    5. the individual score for each file will be printed out